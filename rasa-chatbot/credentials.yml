# Configuration des canaux de communication

# REST channel pour l'intégration avec le backend NestJS
rest:
  # Pas de configuration spécifique nécessaire

# Autres canaux disponibles (commentés par défaut)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# slack:
#   slack_token: "your-slack-token"
#   slack_channel: "your-channel"

# telegram:
#   access_token: "your-telegram-token"
#   verify: "your-verify-token"
#   webhook_url: "your-webhook-url"
